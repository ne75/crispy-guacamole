package main;

import java.util.ArrayList;
import info.gridworld.grid.UnboundedGrid;
import info.gridworld.grid.Location;

public class MyUnboundedGrid<E> extends UnboundedGrid<E> {

    public ArrayList<E> getEmptyAdjacentLocations(Location l){
	ArrayList<E> empty;
	
	empty = getNeighbors(l);
	
	for (int i = 0; i < 8; i++){
	    
	}
	
	return empty;
    }
    
}
